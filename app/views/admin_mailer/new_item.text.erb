<%= @item.user.display_name %> posted a new <%= @item.kind %> on <%= @item.board.name %>:

<%= @item.title %>
<% if @item.text.present? %>

<%= truncate(@item.text, length: 500) %>
<% end %>

Open in the dashboard: <%= dashboard_item_url(@item) %>

<%= render "mailers/admin_footer" %>
